import java.util.Scanner;

class DecimalToBinary {

    public static void main(String[] args) {
        boolean first = true;
        boolean second = false;
        System.out.println(Boolean.compare(first,second));
        // Scanner scnr = new Scanner(System.in);

        // System.out.println("Enter decimal to convert to binary: ");

        // int input_decimal = scnr.nextInt();

        // boolean isRemainder = false;
        // int remainder = 0;

        // while (isRemainder == false) {
        //     remainder = input_decimal 
        // }
    }
}